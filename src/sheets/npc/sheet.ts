export const applyNpcContext = (_sheet: any, _context: any) => {
  // Placeholder for NPC-specific context.
};

export const bindNpcListeners = (_sheet: any, _html: JQuery) => {
  // Placeholder for NPC-specific listeners.
};
